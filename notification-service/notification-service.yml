---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    type: notification-service
---
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: notification-service
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: notification-service
        type: webserver
    spec:
      containers:
        - name: notification-service
          image: parkingsnitch/notification-service:1.0
          env:
            - name: DB_HOST
              value: notification-mongodb
            - name: DB_NAME
              value: notification-service-dbname
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notification-mongodata
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: notification-mongodb
spec:
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    service: notification-mongodb
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: notification-mongodb
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        service: notification-mongodb
    spec:
      containers:
        - image: mongo:3.4
          name: notification-mongodb
          ports:
            - containerPort: 27017
          resources: {}
          volumeMounts:
            - mountPath: /data/db
              name: notification-mongodata
      restartPolicy: Always
      volumes:
        - name: notification-mongodata
          persistentVolumeClaim:
            claimName: notification-mongodata
